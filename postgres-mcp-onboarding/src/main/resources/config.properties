# HTTP Configuration
# HTTP listener host and port for the MCP server
http.host=0.0.0.0
http.port=${http.port}

# Database Configuration
# CloudHub PostgreSQL database connection details
db.url=${db.url}
db.driverClassName=org.postgresql.Driver

# MCP Server Configuration
# MCP server configuration for agent fabric
mcp.serverName=employee-onboarding-mcp-server
mcp.serverVersion=1.0.0

# Security Configuration
# Additional security settings can be added here
