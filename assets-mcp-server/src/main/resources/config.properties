# HTTP Configuration
http.host=0.0.0.0
http.port=8082

# MCP Server Configuration
mcp.serverName=assets-mcp-server
mcp.serverVersion=1.0.0

# CloudHub Configuration (for deployment)
cloudhub.environment=${ANYPOINT_ENV:Sandbox}
cloudhub.region=${CLOUDHUB_REGION:us-east-1}
cloudhub.replicas=${CLOUDHUB_REPLICAS:1}
cloudhub.vcores=${CLOUDHUB_VCORES:0.5}
