# HTTP Configuration
http.port=${HTTP_LISTENER_PORT:8081}

# Application Configuration
app.name=mcp-postgres-server
app.version=1.0.0
app.description=MCP Server for PostgreSQL Employee Database Operations

# Database Configuration (will be overridden by secure properties in CloudHub)
db.host=${DB_HOST:localhost}
db.port=${DB_PORT:5432}
db.name=${DB_NAME:onboarding}
db.username=${DB_USERNAME:mule}
db.password=${DB_PASSWORD:mule123}

# Logging Configuration
logging.level=INFO
